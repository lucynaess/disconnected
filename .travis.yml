language: go

dist: trusty
sudo: false

install:
 - go get github.com/kardianos/govendor
 - govendor get github.com/spf13/hugo

script:
 - hugo --cleanDestinationDir -s disconnected/ -d ../docs --theme=slim --enableGitInfo
